// Voorbeeld in script.js
function initFilters() {
    const filterCat = document.getElementById('filter-cat');
    
    // Deze 'if' zorgt ervoor dat de code alleen draait als de filters op de pagina staan
    if (!filterCat) return; 

    // Hier komt pas je filter-logica
    console.log("Filters gevonden, logica wordt gestart...");
}
